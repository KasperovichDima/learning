B кортеже хранится массив ссылок на элементы.
Напротив, массив в Python - это единственный объект,
в котором хранится C-массив.

У кортежей две функции:
использование в качестве неизменяемых списков и в качестве записей с не-
именованными полями.

tuples support '+'